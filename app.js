(async()=>{const $=s=>document.querySelector(s),A=$('#app'),H=window.QHELP,esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[m])),pct=(a,b)=>b?Math.round(100*a/b):0;let chunks=[];for(let i=1;i<=12;i++){let r=await fetch(`bank-${String(i).padStart(2,'0')}.txt`,{cache:'no-store'});if(!r.ok)throw Error('Fichier banque manquant');chunks.push((await r.text()).trim())}let bin=atob(chunks.join('')),u=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)u[i]=bin.charCodeAt(i);let ds=new DecompressionStream('gzip'),p=JSON.parse(await new Response(new Blob([u]).stream().pipeThrough(ds)).text());const cats=p.c,pref=['HIS','GEO','EMC','ACT','ORG','MAT','LOG','CG'],n=Array(8).fill(0),K=p.k.map(([ci,page,text])=>({ci,page,text,cat:cats[ci],id:`${pref[ci]}-${String(++n[ci]).padStart(4,'0')}`})),EL=['Mathématiques','Raisonnement logique'];let P=JSON.parse(localStorage.getItem('qcm-v6')||'{}'),E=JSON.parse(localStorage.getItem('qcm-ex-v3')||'{}'),route='home',sel=new Set(cats),mode='coverage',type='mixed',size=20,S=[],qi=0;const save=()=>localStorage.setItem('qcm-v6',JSON.stringify(P)),saveE=()=>localStorage.setItem('qcm-ex-v3',JSON.stringify(E)),rec=id=>P[id]||(P[id]={a:0,c:0,w:0,s:0}),st=id=>{let r=P[id];return!r||!r.a?'new':r.s>=2&&r.c>=2?'mastered':r.w&&r.s===0?'error':'seen'};function ks(c){let a=K.filter(x=>x.cat===c),seen=0,m=0,e=0;a.forEach(x=>{let z=st(x.id);if(z!='new')seen++;if(z=='mastered')m++;if(z=='error')e++});return{t:a.length,seen,m,e}}function go(r){route=r;document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('on',b.dataset.r==r));render()}function ex(cat){return H.pick(cat==='Mathématiques'?window.QMATH:window.QLOGIC)()}function home(){let seen=K.filter(x=>st(x.id)!='new').length,m=K.filter(x=>st(x.id)=='mastered').length;A.innerHTML=`<div class="card hero"><h1>Réviser sans laisser de trous</h1><p class="muted">Les 8 cours restent couverts. En mathématiques et raisonnement logique, l’application ajoute maintenant de vrais problèmes à résoudre avec de nouvelles valeurs à chaque session.</p><span class="tag">Couverture documentaire : 100 %</span> <span class="tag practice">Exercices type concours</span><div class="actions"><button class="btn" id="cov">Continuer</button><button class="btn alt" id="err">Mes erreurs</button></div></div><div class="card"><div class="stat">${pct(seen,K.length)} %</div><span class="muted">${seen}/${K.length} informations vues · ${m} maîtrisées</span><div class="bar"><i style="width:${pct(seen,K.length)}%"></i></div></div><div class="grid">${cats.map(c=>{let s=ks(c);return`<div class="card cat" data-cat="${esc(c)}"><b>${esc(c)}</b><span class="muted">${s.t} informations · ${s.e} à revoir</span><div class="bar"><i style="width:${pct(s.seen,s.t)}%"></i></div>${EL.includes(c)?'<span class="tag practice">+ exercices</span>':''}</div>`}).join('')}</div>`;$('#cov').onclick=()=>{mode='coverage';type='mixed';go('quiz')};$('#err').onclick=()=>{mode='errors';type='knowledge';go('quiz')};document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{sel=new Set([b.dataset.cat]);type=EL.includes(b.dataset.cat)?'mixed':'knowledge';go('quiz')})}function quiz(){let has=[...sel].some(c=>EL.includes(c));if(!has&&type!='knowledge')type='knowledge';A.innerHTML=`<h2>Nouvelle session</h2><div class="card"><b>Mode</b><div class="chips">${[['coverage','Couverture'],['errors','Erreurs'],['random','Aléatoire']].map(x=>`<button class="chip ${mode==x[0]?'on':''}" data-m="${x[0]}">${x[1]}</button>`).join('')}</div></div><div class="card"><b>Type de questions</b><div class="chips"><button class="chip ${type=='knowledge'?'on':''}" data-t="knowledge">Connaissances du cours</button><button class="chip ${type=='exercise'?'on':''}" data-t="exercise" ${has?'':'disabled'}>Exercices type concours</button><button class="chip ${type=='mixed'?'on':''}" data-t="mixed" ${has?'':'disabled'}>Mixte</button></div><p class="muted small">Les exercices sont disponibles pour Mathématiques et Raisonnement logique et changent à chaque session.</p></div><div class="card"><b>Matières</b><div class="chips">${cats.map(c=>`<button class="chip ${sel.has(c)?'on':''}" data-c="${esc(c)}">${esc(c)}</button>`).join('')}</div></div><div class="card"><select id="sz" class="field"><option>10</option><option ${size==20?'selected':''}>20</option><option ${size==50?'selected':''}>50</option><option ${size==100?'selected':''}>100</option></select></div><button id="launch" class="btn wide">Lancer</button>`;document.querySelectorAll('[data-m]').forEach(b=>b.onclick=()=>{mode=b.dataset.m;quiz()});document.querySelectorAll('[data-t]').forEach(b=>b.onclick=()=>{if(!b.disabled){type=b.dataset.t;quiz()}});document.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>{let c=b.dataset.c;if(sel.has(c)&&sel.size>1)sel.delete(c);else sel.add(c);quiz()});$('#sz').onchange=e=>size=+e.target.value;$('#launch').onclick=start}function know(count){let a=K.filter(x=>sel.has(x.cat));if(mode=='errors')a=a.filter(x=>P[x.id]?.w);if(mode=='coverage')a.sort((x,y)=>({new:0,error:1,seen:2,mastered:3}[st(x.id)]-{new:0,error:1,seen:2,mastered:3}[st(y.id)])||((P[x.id]?.a||0)-(P[y.id]?.a||0)));else a=H.sh(a);return a.slice(0,count).map(x=>{let d=H.sh(K.filter(y=>y.ci==x.ci&&y.id!=x.id)).slice(0,3).map(y=>y.text),o=H.sh([x.text,...d]);return{kind:'k',cat:x.cat,topic:x.id,prompt:'Quelle affirmation correspond au cours ?',o,ans:o.indexOf(x.text),x,why:x.text}})}function start(){let e=0,k=size;if(type=='exercise'){e=size;k=0}else if(type=='mixed'&&[...sel].some(c=>EL.includes(c))){e=Math.round(size*([...sel].every(c=>EL.includes(c))?.65:.35));k=size-e}let exs=[],ec=[...sel].filter(c=>EL.includes(c));for(let i=0;i<e;i++)exs.push(ex(ec[i%ec.length]));S=H.sh([...know(k),...exs]);qi=0;if(!S.length){A.innerHTML='<div class="card">Aucune question avec ces filtres.</div>';return}show()}function show(){let q=S[qi];A.innerHTML=`<div class="qmeta"><span>${esc(q.cat)} · ${qi+1}/${S.length}</span><span class="tag ${q.kind=='ex'?'practice':''}">${esc(q.topic)}</span></div><div class="bar"><i style="width:${pct(qi,S.length)}%"></i></div><div class="card"><h2>${esc(q.prompt)}</h2>${q.html||''}${q.o.map((x,i)=>`<button class="opt" data-i="${i}">${esc(x)}</button>`).join('')}<div id="fb"></div></div>`;document.querySelectorAll('.opt').forEach(b=>b.onclick=()=>answer(q,+b.dataset.i))}function answer(q,i){let ok=i==q.ans;if(q.kind=='k'){let r=rec(q.x.id);r.a++;ok?(r.c++,r.s++):(r.w++,r.s=0);save()}else{let r=E[q.cat]||(E[q.cat]={a:0,c:0});r.a++;if(ok)r.c++;saveE()}document.querySelectorAll('.opt').forEach((b,j)=>{b.disabled=true;if(j==q.ans)b.classList.add('correct');else if(j==i)b.classList.add('wrong')});$('#fb').innerHTML=`<div class="feedback ${ok?'ok':'ko'}"><b>${ok?'Bonne réponse':'À revoir'}</b><p>${esc(q.why)}</p><small>${q.kind=='ex'?'Exercice original de type concours, généré à partir du cours':'Cours · page '+q.x.page}</small><div class="actions"><button class="btn" id="next">${qi+1<S.length?'Suivante':'Bilan'}</button></div></div>`;$('#next').onclick=()=>{qi++;qi<S.length?show():end()}}function end(){A.innerHTML=`<div class="card hero"><h1>Session terminée</h1><p>${S.length} questions travaillées, dont ${S.filter(x=>x.kind=='ex').length} exercices de résolution.</p><div class="actions"><button class="btn" id="again">Nouvelle session</button><button class="btn alt" id="back">Accueil</button></div></div>`;$('#again').onclick=quiz;$('#back').onclick=()=>go('home')}function inventory(){A.innerHTML=`<h2>Inventaire — ${K.length} informations</h2><p class="muted">Les exercices de maths/logique viennent en plus de cet inventaire : ils ne remplacent aucune information du cours.</p><div class="filters"><input id="q" class="field" placeholder="Rechercher"><select id="cf" class="field"><option value="">Toutes</option>${cats.map(c=>`<option>${esc(c)}</option>`).join('')}</select><select id="sf" class="field"><option value="">Tous statuts</option><option value="new">Jamais vue</option><option value="seen">Vue</option><option value="error">À revoir</option><option value="mastered">Maîtrisée</option></select></div><div id="list"></div>`;['q','cf','sf'].forEach(x=>$('#'+x).oninput=list);list()}function list(){let q=($('#q')?.value||'').toLowerCase(),c=$('#cf')?.value||'',s=$('#sf')?.value||'',a=K.filter(x=>(!c||x.cat==c)&&(!s||st(x.id)==s)&&(!q||x.text.toLowerCase().includes(q))).slice(0,300);$('#list').innerHTML=a.map(x=>`<div class="card row"><code>${x.id} · ${esc(x.cat)} · p.${x.page} · ${st(x.id)}</code><p>${esc(x.text)}</p></div>`).join('')||'<div class="card">Aucun résultat.</div>'}function stats(){let seen=K.filter(x=>st(x.id)!='new').length,m=K.filter(x=>st(x.id)=='mastered').length,ma=E['Mathématiques']||{a:0,c:0},lo=E['Raisonnement logique']||{a:0,c:0};A.innerHTML=`<h2>Progression</h2><div class="grid"><div class="card"><div class="stat">${pct(seen,K.length)}%</div>cours rencontrés</div><div class="card"><div class="stat">${m}</div>maîtrisées</div><div class="card"><div class="stat">${ma.a?pct(ma.c,ma.a):0}%</div>réussite maths (${ma.a} ex.)</div><div class="card"><div class="stat">${lo.a?pct(lo.c,lo.a):0}%</div>réussite logique (${lo.a} ex.)</div></div><button class="btn bad" id="reset">Réinitialiser</button>`;$('#reset').onclick=()=>{if(confirm('Tout effacer ?')){P={};E={};save();saveE();stats()}}}function render(){route=='home'?home():route=='quiz'?quiz():route=='inventory'?inventory():stats()}document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>go(b.dataset.r));render()})().catch(e=>{document.querySelector('#app').innerHTML=`<div class="card"><h2>Impossible de charger l’application</h2><p>${String(e.message||e)}</p><p>Recharge avec Ctrl + Shift + R.</p></div>`;console.error(e)});